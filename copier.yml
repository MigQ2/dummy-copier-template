
dummy_answer:
  type: str
  help: "Dummy answer"
  validator: |-
    {% if dummy_answer == '' %}Answer not provided{% endif %}

dummy_answer_upper:
  type: str
  when: false
  default: |-
    {{ dummy_answer|upper }}

_answers_file: ".copier-answers-{{ dummy_answer }}.yml"
_subdirectory: template


_message_before_copy: |
  You are copying 'The Dummy repo' now
  
  You'll see the rendered templateç
  
_message_after_copy: |
  Template rendered with "{{ dummy_answer }}" successfully
  